# FORM (base image)
# ENV (bien môi trường)
# RUN (chạy shell)
# EXPOSE(mở port từ container đến mạng ảo)
# CMD (chạy comman shell khi container đươcj tạo)
# docker image build (tạo image từ docker file)
FROM node:16

